<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue4</item>
        <item name="colorPrimaryDark">@color/blue5</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/blue2</item>
        <item name="colorControlActivated">@color/blue1</item>
        <item name="colorControlHighlight">@color/blue3</item>
    </style>

    <style name="counterText">
        <item name="android:textColor">@color/colorAccent</item>
    </style>

</resources>
